vault_token: "{{ lookup('ansible.builtin.env', 'vault_token') }}"
secrets: "{{lookup('hashi_vault', 'secret=ci-server/data/ci-secrets token={{ lookup('ansible.builtin.env', 'vault_token') }} url=https://vault.rdevops6a.online:8200 validate_certs=False')}}"